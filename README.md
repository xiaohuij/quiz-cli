# QUIZ SHOW
## Node based quiz command line interactive application

### Install dependencies
npm install

### Run in local
node index -f quiz.json

### Run in global
npm install -g

quiz -f quiz.json